# ?? Diagrama de Flujo - Autenticación y Registro de Mascota

## ?? Flujo Correcto (Lo que DEBERÍA pasar)

```
???????????????????????????????????????????????????????????????????????????
?  FLUTTER APP                                                             ?
???????????????????????????????????????????????????????????????????????????

1. Usuario hace clic en "Login con Google"
   ?
   ??> Firebase Authentication
   ?   ??> Obtiene Firebase ID Token
   ?
2. Flutter intercambia Firebase Token por JWT del Backend
   ?
   POST /api/v1/Auth/firebase
   Body: { "idToken": "firebase_token_here" }
   ?
   ??> ? Backend valida Firebase Token
   ?   ??> Genera JWT propio (accessToken)
   ?
3. Flutter recibe respuesta
   ?
   {
     "success": true,
     "data": {
       "accessToken": "eyJhbGc...",  ? ?? ESTE es el token que debes usar
       "refreshToken": "...",
       "tokenType": "Bearer",
       "usuario": { ... }
     }
   }
   ?
4. Flutter GUARDA el accessToken en Storage
   ?
   await _storageService.saveAccessToken(response.data.accessToken)
   ?
5. ? Token guardado correctamente
   ?
???????????????????????????????????????????????????????????????????????????

AHORA el usuario intenta registrar una mascota:

6. Usuario llena el formulario y presiona "Registrar"
   ?
7. Flutter llama a registrarMascota()
   ?
   mascotaService.registrarMascota(request)
   ?
8. MascotaService llama a ApiService.post()
   ?
   _apiService.post(
     '/MisMascotas',
     body: request.toJson(),
     requiresAuth: true  ? ?? DEBE ser true
   )
   ?
9. ApiService OBTIENE el token del Storage
   ?
   final token = await _storageService.getAccessToken()
   ?
   ??> ? Token encontrado: "eyJhbGc..."
   ?
10. ApiService AGREGA el header Authorization
    ?
    headers['Authorization'] = 'Bearer $token'
    ?
11. ApiService ENVÍA la petición HTTP
    ?
    POST http://192.168.100.11:5151/api/v1/MisMascotas
    Headers:
      Content-Type: application/json
      Authorization: Bearer eyJhbGc...  ? ?? Este header DEBE estar presente
    Body: { "nombre": "...", "especie": "...", ... }
    ?
???????????????????????????????????????????????????????????????????????????

???????????????????????????????????????????????????????????????????????????
?  BACKEND (.NET)                                                          ?
???????????????????????????????????????????????????????????????????????????

12. Backend RECIBE la petición
    ?
    ??> Middleware: AuthenticationLoggingMiddleware
    ?   ??> ?? Loggea: "Authorization Header Presente"
    ?
13. Middleware: UseAuthentication
    ?
    ??> JWT Bearer Handler procesa el token
    ?   ??> ?? OnMessageReceived
    ?   ??> Valida firma, issuer, audience, expiración
    ?   ??> ? OnTokenValidated
    ?       ??> Extrae claims y crea User.Identity
    ?
14. Middleware: UseAuthorization
    ?
    ??> Verifica [Authorize] en el controlador
    ?   ??> ? Usuario autenticado, permite el acceso
    ?
15. MisMascotasController.Create()
    ?
    ??> ?? Loggea claims del usuario
    ?   ??> GetCurrentUserId() extrae el user ID
    ?
16. Servicio crea la mascota
    ?
17. Backend responde
    ?
    HTTP 201 Created
    {
      "success": true,
      "data": { ... mascota creada ... },
      "message": "Mascota registrada exitosamente"
    }
    ?
???????????????????????????????????????????????????????????????????????????

18. ? Flutter recibe la respuesta
    ?
19. ? Muestra mensaje de éxito al usuario
```

---

## ? Lo que ESTÁ pasando (según los logs)

```
???????????????????????????????????????????????????????????????????????????
?  FLUTTER APP                                                             ?
???????????????????????????????????????????????????????????????????????????

1-5. [Login funciona correctamente - ?]

6-7. [Usuario registra mascota - ?]

8-9. MascotaService ? ApiService.post()
     ?
     ?? AQUÍ ESTÁ EL PROBLEMA ??
     ?
     POSIBLE CAUSA A:
     ??> Token NO se guardó en el paso 4
     ?   ??> getAccessToken() devuelve NULL
     ?
     POSIBLE CAUSA B:
     ??> requiresAuth = false (no se intenta obtener token)
     ?
     POSIBLE CAUSA C:
     ??> Token se obtiene pero NO se agrega al header
     ?
10. ApiService envía petición SIN el header Authorization
    ?
    POST http://192.168.100.11:5151/api/v1/MisMascotas
    Headers:
      Content-Type: application/json
      ? Authorization: [NO PRESENTE]
    ?
???????????????????????????????????????????????????????????????????????????

???????????????????????????????????????????????????????????????????????????
?  BACKEND (.NET)                                                          ?
???????????????????????????????????????????????????????????????????????????

11. Backend recibe petición
    ?
    ??> ? AuthenticationLoggingMiddleware NO se ejecuta
    ?   (No hay logs ??)
    ?
12. UseAuthentication
    ?
    ??> JWT Bearer Handler busca el token
    ?   ??> ? No encuentra header Authorization
    ?       ??> User.Identity.IsAuthenticated = false
    ?
13. UseAuthorization
    ?
    ??> Verifica [Authorize]
    ?   ??> ? Usuario NO autenticado
    ?       ??> Rechaza la petición
    ?
14. ? OnChallenge - Responde con 401
    ?
    HTTP 401 Unauthorized
    ?
???????????????????????????????????????????????????????????????????????????

15. ? Flutter recibe 401
    ?
16. ? Muestra error al usuario
```

---

## ?? Puntos de Verificación

### En el Login:
```
? ¿Ves este log?
   "? Token intercambiado exitosamente"
   
? ¿Ves este log?
   "?? Guardando tokens en storage..."
   
? ¿Ves este log?
   "?? Verificación - Token guardado: SÍ"
```

### Al Registrar Mascota (CON los logs de debugging):
```
? ¿Ves este log?
   "?? POST http://192.168.100.11:5151/api/v1/MisMascotas"
   
? ¿Ves este log?
   "?? requiresAuth: true"
   
? ¿Ves este log?
   "Token: SÍ (XXX chars) ?"
   
? ¿Ves este log?
   "?? Headers: Content-Type, Authorization"
```

Si CUALQUIERA de estos logs falta o dice "NULL" o "false", **ahí está tu problema**.

---

## ?? Acción Inmediata

**PASO 1:** Agrega los logs de debugging al `api_service.dart`

**PASO 2:** Ejecuta la app, haz login, intenta registrar mascota

**PASO 3:** Copia TODOS los logs de Flutter aquí

**PASO 4:** Te diré exactamente dónde está fallando
